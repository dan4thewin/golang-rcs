# Add GitHub Actions CI

**Source:** ProjectsToDo (Apr 2026)
**Status:** Still open

No CI pipeline exists in the fork. Consider adding GitHub Actions for:
- `go test ./...`
- `gofmt` check
- `go vet`

This is independent of upstream PR work — useful for the fork itself. May not be needed if upstream already has CI that runs on PRs.